#ifndef TT
#define TT
#include <bits/stdc++.h>
#include "tabl.h"
using namespace std;
namespace std{
	class clavar{
	public:
		int executi;
		bool klmshodf,excellwed;
		double doubnameg;
		long int lintnamep;
		long long int exhjkw,extdf;
		string expere,satu;
		float variab456;
	};
	class vision
	{
	public:
		string get_name(),dumvar231,name;
		map<string,symbol*> ScopeTab,dumvar232,get_tab();
	 	vector<string> dumvar233,err_checker;	
		vision(string name_v);
		virtual ~vision(); 
		void insert_record(string,symbol*);
	};
}
#endif
